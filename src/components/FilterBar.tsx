"use client";
import { SlidersHorizontal } from "lucide-react";
type Props = { tag: string; rating: string; from: string; to: string; rewatch: boolean; country: string; language: string; genre: string; tags: string[]; countries: string[]; languages: string[]; genres: string[]; onChange: (key: string, value: string | boolean) => void };
export function FilterBar({ tag, rating, from, to, rewatch, country, language, genre, tags, countries, languages, genres, onChange }: Props) { return <details className="group mt-4 rounded border border-[var(--line)] bg-[var(--panel)]"><summary className="flex cursor-pointer list-none items-center gap-2 px-4 py-3 text-sm font-semibold text-[var(--muted)]"><SlidersHorizontal size={16} className="text-[var(--accent)]" /> Find the right film <span className="ml-auto text-xs group-open:hidden">Filters</span></summary><div className="grid gap-3 border-t border-[var(--line)] p-4 sm:grid-cols-2 lg:grid-cols-4"><FilterSelect label="Tag" value={tag} options={tags} onChange={(value) => onChange("tag", value)} /><FilterSelect label="Minimum rating" value={rating} options={["5", "4", "3", "2", "1"]} optionLabel={(value) => `${value}+ stars`} onChange={(value) => onChange("rating", value)} /><FilterSelect label="Country" value={country} options={countries} onChange={(value) => onChange("country", value)} /><FilterSelect label="Language" value={language} options={languages} onChange={(value) => onChange("language", value)} /><FilterSelect label="Genre" value={genre} options={genres} onChange={(value) => onChange("genre", value)} /><label className="label">Watched after<input className="field mt-2" type="date" value={from} onChange={(e) => onChange("from", e.target.value)} /></label><label className="label">Watched before<input className="field mt-2" type="date" value={to} onChange={(e) => onChange("to", e.target.value)} /></label><label className="flex items-center gap-3 self-end pb-3 text-sm text-[var(--paper)]"><input type="checkbox" checked={rewatch} onChange={(e) => onChange("rewatch", e.target.checked)} className="accent-[var(--accent)]" /> Rewatches only</label></div></details>; }
function FilterSelect({ label, value, options, optionLabel = (item: string) => item, onChange }: { label: string; value: string; options: string[]; optionLabel?: (value: string) => string; onChange: (value: string) => void }) { return <label className="label">{label}<select className="field mt-2" value={value} onChange={(e) => onChange(e.target.value)}><option value="">Any {label.toLowerCase()}</option>{options.map((item) => <option key={item} value={item}>{optionLabel(item)}</option>)}</select></label>; }